- To recompile the image run: `docker compose build`
- To execute run: `docker compose run compile`
- Docker compose is mounting the directory latex-src onto the vm so move your files in there before starting
